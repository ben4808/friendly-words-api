PORT=3001
# Point at an already-running, initialized database.
# From inside the container, use host.docker.internal to reach a DB on the host,
# or the service name of a DB in another compose network.
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/cruzi
